export default (theme) => ({
    root: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        textAlign: 'center',
        textDecoration: 'none',
        color: theme.palette.white,
        fontSize: '1rem',
        fontWeight: 'normal',
    },
});
